# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.lock

# Generated localization output (regenerated automatically by `flutter pub get`)
lib/l10n/app_localizations*.dart

# Android signing / secrets — NEVER commit these
android/key.properties
android/local.properties
android/app/google-services.json
android/app/*.jks
android/app/*.keystore
*.jks
*.keystore

# IDE
.idea/
*.iml

# Firebase (regenerate via `flutterfire configure`, do not hand-edit a committed version with real keys)
# lib/firebase_options.dart is intentionally committed as a placeholder — see its header comment.
